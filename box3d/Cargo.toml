[package]
name = "box3d"
version = "0.1.13"
edition = "2021"
description = "Safe Rust bindings for Box3D."
license = "MIT"
readme = "README.md"
repository = "https://github.com/Tebarem/box3d-rs"

[dependencies]
box3d-sys = { path = "..", version = "0.1.13" }
